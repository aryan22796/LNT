# Function with ChainMap and File Inputs
# Let's create a function that combines configurations from two
# files and allows for additional overrides via a dictionary passed as a parameter.
# Default configurations
default_config = {
    'host': 'localhost',
    'port': 8080,
    'debug': False
}
